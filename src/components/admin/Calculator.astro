---
---

<div class="page">
  <div class="portal-content">

    <div class="section-label">Property Financial Analyzer</div>

    <div class="calc-layout">

      <!-- INPUT PANEL -->
      <div class="calc-panel">
        <div class="calc-panel-title">
          Inputs
          <span id="current-analysis-id" style="display:none"></span>
        </div>

        <!-- Property Association -->
        <div class="property-assoc">
          <div class="property-assoc-row">
            <div class="field-group" style="flex:1;min-width:0">
              <label for="inp-property">Linked Property</label>
              <select id="inp-property">
                <option value="">— Select a property —</option>
              </select>
            </div>
            <button id="btn-new-property" type="button" class="btn-add-prop" title="Add new property">
              <svg viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
              New
            </button>
          </div>
          <div id="orphan-warning" class="assoc-banner warn" style="display:none">
            This analysis has no linked property. Select one above before saving.
          </div>
          <div id="no-property-hint" class="assoc-banner info" style="display:none">
            Select a property above to enable saving.
          </div>
          <div id="no-properties-hint" class="assoc-banner info" style="display:none">
            No properties yet. <a href="/admin/properties">Create one first →</a>
          </div>
        </div>

        <!-- Section 1: Property Basics -->
        <div class="calc-section">
          <button class="calc-section-toggle open" data-target="sec-basics" type="button">
            Property Basics
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body open" id="sec-basics">
            <div class="field-row full">
              <div class="field-group">
                <label for="inp-name">Analysis Label</label>
                <input type="text" id="inp-name" value="New Analysis" placeholder="e.g. 1234 Oak St — Summer" />
              </div>
            </div>
            <div class="field-row full">
              <div class="field-group">
                <label for="inp-address">Property Address</label>
                <input type="text" id="inp-address" placeholder="Auto-filled from linked property" readonly />
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-bedrooms">Bedrooms</label>
                <input type="text" id="inp-bedrooms" disabled class="prop-auto-field" placeholder="— from property —" />
              </div>
              <div class="field-group">
                <label for="inp-bathrooms">Bathrooms</label>
                <input type="text" id="inp-bathrooms" disabled class="prop-auto-field" placeholder="— from property —" />
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-type">Property Type</label>
                <input type="text" id="inp-type" disabled class="prop-auto-field" placeholder="— from property —" />
              </div>
              <div class="field-group"></div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-rent-ask">Monthly Rent (Ask)</label>
                <input type="number" id="inp-rent-ask" value="1400" min="0" step="50" />
              </div>
              <div class="field-group">
                <label for="inp-rent-neg">Monthly Rent (Negotiated)</label>
                <input type="number" id="inp-rent-neg" value="1200" min="0" step="50" />
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-deposit">Security Deposit</label>
                <input type="number" id="inp-deposit" value="1400" min="0" step="50" />
              </div>
              <div class="field-group">
                <label for="inp-hoa">HOA Present?</label>
                <select id="inp-hoa">
                  <option value="No" selected>No</option>
                  <option value="Yes">Yes</option>
                </select>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-walk-away">Walk-Away Threshold ($)</label>
                <input type="number" id="inp-walk-away" value="1750" min="0" step="50" />
                <span class="field-note">Max monthly rent R&amp;L will sign</span>
              </div>
              <div class="field-group"></div>
            </div>
            <div class="hoa-block" id="hoa-block">
              <div class="hoa-block-title">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
                HOA Detected — Property Disqualified
              </div>
              <p class="hoa-block-body">Vinclo Real Estate does not lease HOA properties under any circumstances.</p>
            </div>
          </div>
        </div>

        <!-- Section 2: Revenue Assumptions -->
        <div class="calc-section">
          <button class="calc-section-toggle open" data-target="sec-revenue" type="button">
            Revenue Assumptions
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body open" id="sec-revenue">
            <div class="field-row">
              <div class="field-group">
                <label for="inp-adr">ADR — Avg Daily Rate ($)</label>
                <input type="number" id="inp-adr" value="167" min="0" step="1" />
                <span class="field-note">AirDNA Abilene 2BR avg</span>
              </div>
              <div class="field-group">
                <label for="inp-occ">Occupancy Rate (%)</label>
                <input type="number" id="inp-occ" value="77" min="0" max="100" step="1" />
                <span class="field-note">AirDNA Abilene annual avg</span>
                <span class="field-note occ-cap-note" id="occ-cap-note" style="display:none">capped at 95% — practical ceiling</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-stay">Avg Stay Length (nights)</label>
                <input type="number" id="inp-stay" value="3.2" min="1" step="0.1" />
                <span class="field-note">Abilene military/university profile</span>
              </div>
              <div class="field-group">
                <label for="inp-demand">Primary Demand Driver</label>
                <select id="inp-demand">
                  <option value="Military TDY" selected>Military TDY</option>
                  <option value="University Events">University Events</option>
                  <option value="Mixed">Mixed</option>
                  <option value="Corporate/Contractor">Corporate / Contractor</option>
                </select>
              </div>
            </div>
          </div>
        </div>

        <!-- Section 2b: New-Listing Ramp -->
        <div class="calc-section">
          <button class="calc-section-toggle open" data-target="sec-ramp" type="button">
            New-Listing Ramp
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body open" id="sec-ramp">
            <div class="field-row">
              <div class="field-group">
                <label for="inp-ramp-enabled">Ramp Mode</label>
                <select id="inp-ramp-enabled">
                  <option value="Yes" selected>On — realistic year 1</option>
                  <option value="No">Off — day-1 stabilized</option>
                </select>
                <span class="field-note">Models gradual occupancy/ADR ramp for a new listing</span>
              </div>
              <div class="field-group">
                <label for="inp-payback-rule">Payback Rule (months)</label>
                <input type="number" id="inp-payback-rule" value="9" min="1" step="1" />
                <span class="field-note">Ramped payback beyond this fires a risk flag</span>
              </div>
            </div>
            <div id="ramp-fields">
              <div class="field-row">
                <div class="field-group">
                  <label for="inp-make-ready">Make-Ready Month 1?</label>
                  <select id="inp-make-ready">
                    <option value="Yes" selected>Yes — zero revenue</option>
                    <option value="No">No</option>
                  </select>
                  <span class="field-note">Setup, photography, listing go-live</span>
                </div>
                <div class="field-group">
                  <label for="inp-rent-free">Month 1 Rent-Free?</label>
                  <select id="inp-rent-free">
                    <option value="Yes" selected>Yes — negotiated</option>
                    <option value="No">No — adds rent to carry</option>
                  </select>
                </div>
              </div>
              <div class="calc-subsection-title">Ramp Phases (× base occ / ADR)</div>
              <div class="field-row ramp-phase-row">
                <div class="field-group">
                  <label for="inp-ramp1-months">Phase 1 — Months</label>
                  <input type="number" id="inp-ramp1-months" value="3" min="0" step="1" />
                </div>
                <div class="field-group">
                  <label for="inp-ramp1-occ">Occ Factor</label>
                  <input type="number" id="inp-ramp1-occ" value="0.85" min="0" max="1.5" step="0.01" />
                </div>
                <div class="field-group">
                  <label for="inp-ramp1-adr">ADR Factor</label>
                  <input type="number" id="inp-ramp1-adr" value="0.90" min="0" max="1.5" step="0.01" />
                </div>
              </div>
              <div class="field-row ramp-phase-row">
                <div class="field-group">
                  <label for="inp-ramp2-months">Phase 2 — Months</label>
                  <input type="number" id="inp-ramp2-months" value="3" min="0" step="1" />
                </div>
                <div class="field-group">
                  <label for="inp-ramp2-occ">Occ Factor</label>
                  <input type="number" id="inp-ramp2-occ" value="0.93" min="0" max="1.5" step="0.01" />
                </div>
                <div class="field-group">
                  <label for="inp-ramp2-adr">ADR Factor</label>
                  <input type="number" id="inp-ramp2-adr" value="0.96" min="0" max="1.5" step="0.01" />
                </div>
              </div>
              <span class="field-note">Final phase: stabilized at 1.00 / 1.00. Occupancy is capped at 95% after factors.</span>
            </div>
          </div>
        </div>

        <!-- Section 3: Fixed Monthly Costs -->
        <div class="calc-section">
          <button class="calc-section-toggle open" data-target="sec-fixed" type="button">
            Fixed Monthly Costs
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body open" id="sec-fixed">

            <div class="calc-subsection-title">Utilities</div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-electricity">Electricity (AEP Texas)</label>
                <input type="number" id="inp-electricity" value="130" min="0" step="1" />
                <span class="field-note">$120–$140 (2BR) · $145–$170 (3BR)</span>
              </div>
              <div class="field-group">
                <label for="inp-water">Water</label>
                <input type="number" id="inp-water" value="45" min="0" step="1" />
                <span class="field-note">$40–$50 (2BR) · $50–$60 (3BR) · confirm if landlord-included</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-sewer">Sewer</label>
                <input type="number" id="inp-sewer" value="50" min="0" step="1" />
                <span class="field-note">$45–$55 (2BR) · $55–$65 (3BR) · confirm if landlord-included</span>
              </div>
              <div class="field-group">
                <label for="inp-garbage">Garbage (City of Abilene)</label>
                <input type="number" id="inp-garbage" value="24" min="0" step="1" />
                <span class="field-note">$24 flat · confirm if landlord-included</span>
              </div>
            </div>

            <div class="calc-subsection-title">Internet &amp; Insurance</div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-internet">Internet (fiber)</label>
                <input type="number" id="inp-internet" value="80" min="0" step="1" />
              </div>
              <div class="field-group">
                <label for="inp-insurance">STR Insurance (Proper)</label>
                <input type="number" id="inp-insurance" value="100" min="0" step="1" />
                <span class="field-note">Quote in hand: $98–103/mo</span>
              </div>
            </div>

            <div class="calc-subsection-title">Supplies</div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-supplies">Supplies &amp; Amenities</label>
                <input type="number" id="inp-supplies" value="120" min="0" step="5" />
              </div>
              <div class="field-group">
                <label for="inp-linens">Linens &amp; Towels (reserve)</label>
                <input type="number" id="inp-linens" value="41" min="0" step="1" />
                <span class="field-note">$35 (2BR) · $48 (3BR)</span>
              </div>
            </div>

            <div class="calc-subsection-title">Tech &amp; Platforms</div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-pms">PMS Software (Hospitable)</label>
                <input type="number" id="inp-pms" value="39" min="0" step="1" />
              </div>
              <div class="field-group">
                <label for="inp-pricing">Dynamic Pricing (PriceLabs)</label>
                <input type="number" id="inp-pricing" value="20" min="0" step="1" />
                <span class="field-note">$19.99/listing/mo after trial</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-minut-subscription">Minut (noise sensor)</label>
                <input type="number" id="inp-minut-subscription" value="10" min="0" step="1" />
                <span class="field-note">$10 flat</span>
              </div>
              <div class="field-group">
                <label for="inp-streaming">Streaming (Netflix/Hulu)</label>
                <input type="number" id="inp-streaming" value="8" min="0" step="1" />
                <span class="field-note">$8 flat · guest profile</span>
              </div>
            </div>

            <div class="calc-subsection-title">Maintenance &amp; Admin</div>
            <div class="field-row">
              <div class="field-group">
                <label>Maintenance Reserve</label>
                <input type="number" id="inp-maintenance" readonly />
                <span class="field-note">Auto: 1.5% of negotiated rent (min $75, max $150)</span>
              </div>
              <div class="field-group">
                <label for="inp-preventive-inspection">Preventive Inspection</label>
                <input type="number" id="inp-preventive-inspection" value="50" min="0" step="1" />
                <span class="field-note">$50 · handyman · every 2 mo (6x/yr) · prorated</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-hvac-filters">HVAC Filters</label>
                <input type="number" id="inp-hvac-filters" value="10" min="0" step="1" />
                <span class="field-note">$8 (2BR) · $12 (3BR) · every 45–60 days · prorated</span>
              </div>
              <div class="field-group">
                <label for="inp-cpa">CPA / Taxes</label>
                <input type="number" id="inp-cpa" value="42" min="0" step="1" />
                <span class="field-note">~$500/yr billed Q1 · prorated</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label>STR Permit</label>
                <input type="number" value="0" readonly disabled />
                <span class="field-note"><span class="field-badge">No permit required — City of Abilene (2026)</span></span>
              </div>
              <div class="field-group"></div>
            </div>
          </div>
        </div>

        <!-- Section 3b: Property Services -->
        <div class="calc-section">
          <button class="calc-section-toggle" data-target="sec-property-services" type="button">
            Property Services
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body" id="sec-property-services">
            <div class="field-row">
              <div class="field-group">
                <label for="inp-has-yard">Has Yard?</label>
                <select id="inp-has-yard">
                  <option value="No" selected>No</option>
                  <option value="Yes">Yes</option>
                </select>
                <span class="field-note">Enables lawn care cost when Yes</span>
              </div>
              <div class="field-group">
                <label for="inp-lawn-care">Lawn Care</label>
                <input type="number" id="inp-lawn-care" value="0" min="0" step="1" readonly />
                <span class="field-note">$75 (2BR) · $82 (3BR) · Apr–Oct 2x/mo, Nov–Mar 1x/mo · prorated</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-pest-control">Pest Control</label>
                <input type="number" id="inp-pest-control" value="51" min="0" step="1" />
                <span class="field-note">$50 (2BR) · $53 (3BR) · quarterly · prorated</span>
              </div>
              <div class="field-group">
                <label for="inp-bulk-pickup">Bulk Pickup</label>
                <input type="number" id="inp-bulk-pickup" value="8" min="0" step="1" />
                <span class="field-note">~2x/yr · prorated</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Section 4: Per-Stay Variable Costs -->
        <div class="calc-section">
          <button class="calc-section-toggle" data-target="sec-variable" type="button">
            Per-Stay Variable Costs
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body" id="sec-variable">
            <div class="field-row">
              <div class="field-group">
                <label for="inp-cleaning">Cleaning Fee per Stay ($)</label>
                <input type="number" id="inp-cleaning" value="120" min="0" step="5" />
                <span class="field-note">Pass-through: charged to guest = paid to cleaner. Net P&amp;L impact = $0.</span>
              </div>
              <div class="field-group">
                <label for="inp-airbnb-fee-type">Airbnb Host Fee</label>
                <select id="inp-airbnb-fee-type">
                  <option value="3%">3% — Direct (no API)</option>
                  <option value="15.5%">15.5% — Hospitable API</option>
                </select>
                <span class="field-note">Verify in Airbnb → Payments &amp; Payouts. API-connected = 15.5%.</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Section 5: Setup & One-Time Costs -->
        <div class="calc-section">
          <button class="calc-section-toggle" data-target="sec-setup" type="button">
            Setup &amp; One-Time Costs
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body" id="sec-setup">
            <div class="field-row">
              <div class="field-group">
                <label for="inp-furniture">Furniture &amp; Décor</label>
                <input type="number" id="inp-furniture" value="6500" min="0" step="100" />
                <span class="field-note">2BR standard</span>
              </div>
              <div class="field-group">
                <label for="inp-photo">Photography</label>
                <input type="number" id="inp-photo" value="205" min="0" step="50" />
                <span class="field-note">$180–$230 (2BR)</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-lock">Smart Lock</label>
                <input type="number" id="inp-lock" value="180" min="0" step="10" />
                <span class="field-note">Schlage/Yale</span>
              </div>
              <div class="field-group">
                <label for="inp-minut-hardware">Minut Hardware</label>
                <input type="number" id="inp-minut-hardware" value="100" min="0" step="10" />
                <span class="field-note">$100 flat</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-wifi-router">WiFi Router (premium)</label>
                <input type="number" id="inp-wifi-router" value="100" min="0" step="10" />
                <span class="field-note">$80 (2BR) · $120 (3BR)</span>
              </div>
              <div class="field-group">
                <label for="inp-welcome-kits">Welcome Kits (2-mo supply)</label>
                <input type="number" id="inp-welcome-kits" value="195" min="0" step="5" />
                <span class="field-note">$170 (2BR) · $220 (3BR)</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label>LLC Formation</label>
                <input type="number" value="0" readonly disabled />
                <span class="field-note">Already registered — locked at $0</span>
              </div>
              <div class="field-group">
                <label for="inp-legal">Legal (Sublease Addendum)</label>
                <input type="number" id="inp-legal" value="400" min="0" step="50" />
                <span class="field-note">TX attorney quote</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-misc">Miscellaneous Buffer</label>
                <input type="number" id="inp-misc" value="500" min="0" step="50" />
              </div>
              <div class="field-group"></div>
            </div>
          </div>
        </div>

        <!-- Section 6: HOT Tax Info -->
        <div class="calc-section">
          <button class="calc-section-toggle" data-target="sec-hot" type="button">
            HOT Tax (Abilene)
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body" id="sec-hot">
            <div class="hot-info">
              <p><strong>Hotel Occupancy Tax (HOT) — Abilene, TX</strong><br />
              HOT tax is collected and remitted directly by Airbnb on behalf of the host.
              It does not appear as a cost to the operator and does not impact the P&amp;L model.
              No action required.</p>
            </div>
          </div>
        </div>

        <!-- Notes -->
        <div class="calc-section">
          <button class="calc-section-toggle" data-target="sec-notes" type="button">
            Notes
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body" id="sec-notes">
            <div class="field-row full">
              <div class="field-group">
                <label for="inp-notes">Notes</label>
                <textarea id="inp-notes" placeholder="Landlord contact, property condition, negotiation notes…"></textarea>
              </div>
            </div>
          </div>
        </div>

      </div><!-- /calc-panel -->

      <!-- OUTPUT PANEL -->
      <div class="output-panel" id="output-panel">

        <div class="hoa-block" id="hoa-save-block">
          <div class="hoa-block-title">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
            HOA Detected — Property Disqualified
          </div>
          <p class="hoa-block-body">Saving and export are disabled. Vinclo Real Estate does not lease HOA properties.</p>
        </div>

        <!-- Section 1: Property header -->
        <div class="res-header">
          <div class="res-header-main">
            <div class="res-address" id="res-address">—</div>
            <div class="res-subline" id="res-subline">Select a property to begin</div>
          </div>
          <div class="res-header-side">
            <div class="res-buffer-label">Break-even buffer</div>
            <div class="res-buffer-val" id="res-buffer">—</div>
          </div>
        </div>
        <div id="res-badge" class="res-badge" style="display:none"></div>

        <!-- Section 2: Metric cards -->
        <div class="res-metrics">
          <div class="res-metric" id="rmc-net-monthly">
            <div class="rmc-val" id="res-net-monthly">—</div>
            <div class="rmc-label">Net / month</div>
            <div class="rmc-sub" id="res-margin-sub">— margin</div>
          </div>
          <div class="res-metric" id="rmc-net-annual">
            <div class="rmc-val" id="res-net-annual">—</div>
            <div class="rmc-label">Net / year</div>
            <div class="rmc-sub">base case</div>
          </div>
          <div class="res-metric">
            <div class="rmc-val" id="res-roi12">—</div>
            <div class="rmc-label">12-mo ROI</div>
            <div class="rmc-sub" id="res-roi-sub">—</div>
          </div>
          <div class="res-metric">
            <div class="rmc-val" id="res-payback">—</div>
            <div class="rmc-label">Payback</div>
            <div class="rmc-sub" id="res-mult-sub">—</div>
          </div>
        </div>

        <!-- 1/3 Rule Validator -->
        <div class="res-chart-card rule-card">
          <div class="rule-card-hd">
            <span class="rcc-title" style="margin-bottom:0">1/3 Rule Validator</span>
            <span class="rule-target">target: each bucket ≈ 33% of gross</span>
          </div>
          <div class="rule-legend">
            <div class="rule-legend-item">
              <span class="rule-swatch" id="rule-swatch-rent"></span>
              <span class="rule-legend-label">Rent</span>
              <span class="rule-legend-pct" id="rule-rent-pct">—</span>
            </div>
            <div class="rule-legend-item">
              <span class="rule-swatch" id="rule-swatch-ops"></span>
              <span class="rule-legend-label">Operating</span>
              <span class="rule-legend-pct" id="rule-ops-pct">—</span>
            </div>
            <div class="rule-legend-item">
              <span class="rule-swatch" id="rule-swatch-net"></span>
              <span class="rule-legend-label">Net</span>
              <span class="rule-legend-pct" id="rule-net-pct">—</span>
            </div>
          </div>
          <div class="rule-bar">
            <div class="rule-segment" id="rule-seg-rent"></div>
            <div class="rule-segment" id="rule-seg-ops"></div>
            <div class="rule-segment" id="rule-seg-net"></div>
          </div>
          <div class="rule-verdict-row">
            <span class="rule-verdict" id="rule-verdict">—</span>
          </div>
        </div>

        <!-- Section 3: Two-column chart row -->
        <div class="res-chart-row">
          <div class="res-chart-card">
            <div class="rcc-title">Revenue waterfall</div>
            <div style="position:relative;height:220px">
              <canvas id="chart-waterfall"></canvas>
            </div>
          </div>
          <div class="res-chart-card">
            <div class="rcc-title">Break-even vs actual occupancy</div>
            <div class="gauge-wrap">
              <canvas id="chart-gauge" width="220" height="130"></canvas>
            </div>
            <div class="gauge-legend" id="gauge-legend"></div>
            <div class="rcc-title" style="margin-top:14px">Scenario outcomes</div>
            <div style="position:relative;height:80px">
              <canvas id="chart-scenarios"></canvas>
            </div>
          </div>
        </div>

        <!-- Section 4: Monthly cash flow -->
        <div class="res-chart-card">
          <div class="rcc-title">Estimated monthly cash flow</div>
          <div style="position:relative;height:140px">
            <canvas id="chart-monthly"></canvas>
          </div>
          <div class="rcc-footnote" id="monthly-footnote"></div>
          <div class="rcc-market-note" id="market-note"></div>
        </div>

        <!-- Section 5: ROI curve -->
        <div class="res-chart-card">
          <div class="rcc-title">Cumulative ROI — 24-month payback curve</div>
          <div style="position:relative;height:140px">
            <canvas id="chart-roi"></canvas>
          </div>
          <div class="rcc-footnote" id="roi-footnote"></div>
        </div>

        <!-- P&L Statement -->
        <div class="res-chart-card">
          <div class="rcc-title">Monthly P&amp;L Statement — Base Case</div>
          <table class="pl-table">
            <tbody>
              <tr class="pl-section-hd"><td colspan="2">Revenue</td></tr>
              <tr><td>Gross Revenue (ADR × Occ × 30 nights)</td><td id="pl-gross-revenue" class="pl-amount"></td></tr>
              <tr class="pl-section-hd"><td colspan="2">Cost of Revenue</td></tr>
              <tr><td id="pl-airbnb-label">Airbnb Host Fee (3%)</td><td id="pl-airbnb-fee" class="pl-amount pl-cost"></td></tr>
              <tr class="pl-divider"><td colspan="2"></td></tr>
              <tr class="pl-subtotal"><td>Net Platform Revenue</td><td id="pl-net-platform" class="pl-amount"></td></tr>
              <tr class="pl-section-hd"><td colspan="2">Fixed Costs</td></tr>
              <tr><td>Rent</td><td id="pl-rent" class="pl-amount pl-cost"></td></tr>
              <tr><td>Electricity</td><td id="pl-electricity" class="pl-amount pl-cost"></td></tr>
              <tr><td>Water</td><td id="pl-water" class="pl-amount pl-cost"></td></tr>
              <tr><td>Sewer</td><td id="pl-sewer" class="pl-amount pl-cost"></td></tr>
              <tr><td>Garbage</td><td id="pl-garbage" class="pl-amount pl-cost"></td></tr>
              <tr><td>Internet</td><td id="pl-internet" class="pl-amount pl-cost"></td></tr>
              <tr><td>STR Insurance (Proper)</td><td id="pl-insurance" class="pl-amount pl-cost"></td></tr>
              <tr><td>Supplies &amp; Amenities</td><td id="pl-supplies" class="pl-amount pl-cost"></td></tr>
              <tr><td>Linens &amp; Towels</td><td id="pl-linens" class="pl-amount pl-cost"></td></tr>
              <tr><td>Hospitable PMS</td><td id="pl-pms" class="pl-amount pl-cost"></td></tr>
              <tr><td>PriceLabs</td><td id="pl-pricing" class="pl-amount pl-cost"></td></tr>
              <tr><td>Minut Subscription</td><td id="pl-minut" class="pl-amount pl-cost"></td></tr>
              <tr><td>Streaming</td><td id="pl-streaming" class="pl-amount pl-cost"></td></tr>
              <tr><td>Lawn Care</td><td id="pl-lawn-care" class="pl-amount pl-cost"></td></tr>
              <tr><td>Pest Control</td><td id="pl-pest-control" class="pl-amount pl-cost"></td></tr>
              <tr><td>Bulk Pickup</td><td id="pl-bulk-pickup" class="pl-amount pl-cost"></td></tr>
              <tr><td>Maintenance Reserve</td><td id="pl-maintenance" class="pl-amount pl-cost"></td></tr>
              <tr><td>Preventive Inspection</td><td id="pl-preventive-inspection" class="pl-amount pl-cost"></td></tr>
              <tr><td>HVAC Filters</td><td id="pl-hvac-filters" class="pl-amount pl-cost"></td></tr>
              <tr><td>CPA / Taxes</td><td id="pl-cpa" class="pl-amount pl-cost"></td></tr>
              <tr class="pl-divider"><td colspan="2"></td></tr>
              <tr class="pl-subtotal"><td>Total Monthly Costs</td><td id="pl-total-costs" class="pl-amount pl-cost"></td></tr>
              <tr class="pl-divider pl-divider--thick"><td colspan="2"></td></tr>
              <tr class="pl-net"><td>Net Monthly Profit</td><td id="pl-net-monthly" class="pl-amount"></td></tr>
              <tr class="pl-net"><td>Net Annual Profit</td><td id="pl-net-annual" class="pl-amount"></td></tr>
              <tr class="pl-divider"><td colspan="2"></td></tr>
              <tr class="pl-kpi"><td>Break-Even Occupancy</td><td id="pl-breakeven" class="pl-amount"></td></tr>
              <tr class="pl-kpi"><td>Profit Margin</td><td id="pl-margin" class="pl-amount"></td></tr>
            </tbody>
          </table>
        </div>

        <!-- Initial Investment -->
        <div class="res-chart-card">
          <div class="rcc-title">Initial Investment</div>
          <table class="pl-table">
            <tbody>
              <tr class="pl-section-hd"><td colspan="2">Capital Required</td></tr>
              <tr><td>Security Deposit</td><td id="ii-deposit" class="pl-amount pl-cost"></td></tr>
              <tr><td>Furniture &amp; Décor</td><td id="ii-furniture" class="pl-amount pl-cost"></td></tr>
              <tr><td>Photography</td><td id="ii-photo" class="pl-amount pl-cost"></td></tr>
              <tr><td>Smart Lock</td><td id="ii-lock" class="pl-amount pl-cost"></td></tr>
              <tr><td>Minut Hardware</td><td id="ii-minut-hardware" class="pl-amount pl-cost"></td></tr>
              <tr><td>WiFi Router</td><td id="ii-wifi-router" class="pl-amount pl-cost"></td></tr>
              <tr><td>Welcome Kits</td><td id="ii-welcome-kits" class="pl-amount pl-cost"></td></tr>
              <tr><td>Legal</td><td id="ii-legal" class="pl-amount pl-cost"></td></tr>
              <tr><td>Miscellaneous</td><td id="ii-misc" class="pl-amount pl-cost"></td></tr>
              <tr id="ii-carry-row" style="display:none"><td>Month-1 Carry (auto — hover for breakdown)</td><td id="ii-carry" class="pl-amount pl-cost"></td></tr>
              <tr class="pl-divider pl-divider--thick"><td colspan="2"></td></tr>
              <tr class="pl-subtotal"><td>Total Initial Investment</td><td id="ii-total" class="pl-amount pl-cost"></td></tr>
            </tbody>
          </table>
          <div class="ii-metrics">
            <div class="res-metric">
              <div class="rmc-val" id="ii-payback">—</div>
              <div class="rmc-label" id="ii-payback-label">Payback Period</div>
              <div class="rmc-sub" id="ii-payback-flat" style="display:none" title="assumes day-1 stabilization — optimistic"></div>
            </div>
            <div class="res-metric">
              <div class="rmc-val" id="ii-roi12">—</div>
              <div class="rmc-label">12-Month ROI</div>
            </div>
            <div class="res-metric">
              <div class="rmc-val" id="ii-roi24">—</div>
              <div class="rmc-label">24-Month ROI</div>
            </div>
          </div>
        </div>

        <!-- Risk flags (collapsible) -->
        <details class="res-collapsible">
          <summary class="res-coll-hd">
            <span>Risk Assessment</span>
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </summary>
          <div class="res-coll-body">
            <div class="risk-flags" id="risk-flags">
              <div class="risk-flag ok"><span class="risk-flag-icon">✓</span>No risk flags triggered.</div>
            </div>
          </div>
        </details>

        <!-- Cleaning pass-through (collapsible) -->
        <details class="res-collapsible">
          <summary class="res-coll-hd">
            <span>Cleaning — Pass-Through</span>
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </summary>
          <div class="res-coll-body">
            <div class="passthrough-note">Revenue collected from guest = cost paid to cleaner. Net P&amp;L impact = $0.</div>
            <div class="metric-row"><span class="metric-label">Stays / Month</span><span class="metric-value" id="out-stays">—</span></div>
            <div class="metric-row"><span class="metric-label">Cleaning Collected</span><span class="metric-value" id="out-cleaning-collected">—</span></div>
            <div class="metric-row"><span class="metric-label">Cleaning Paid</span><span class="metric-value" id="out-cleaning-paid">—</span></div>
            <div class="metric-row"><span class="metric-label">Net P&amp;L Impact</span><span class="metric-value green">$0</span></div>
          </div>
        </details>

        <!-- Actions -->
        <div class="calc-actions">
          <button class="btn btn-primary" id="btn-save" type="button" disabled>
            <svg viewBox="0 0 24 24"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>
            Save Analysis
          </button>
          <button class="btn btn-ghost" id="btn-update" type="button" style="display:none">
            <svg viewBox="0 0 24 24"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>
            Update
          </button>
          <button class="btn btn-ghost" id="btn-new" type="button" style="display:none">New Analysis</button>
          <button class="btn btn-ghost" id="btn-export" type="button">
            <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
            Export .md
          </button>
        </div>

        <div class="view-saved-link">
          <a href="/admin/analyses">View all saved analyses →</a>
        </div>

      </div><!-- /output-panel -->
    </div><!-- /calc-layout -->

  </div><!-- /portal-content -->
</div><!-- /page -->

<footer>
  <div class="footer-brand">Vinclo Real Estate</div>
  <div class="footer-meta">&copy; 2026 &middot; Abilene, TX &middot; Owner Portal</div>
</footer>

<!-- New Property Modal -->
<div class="modal-backdrop" id="modal-backdrop" style="display:none"></div>
<div class="modal" id="modal-new-property" style="display:none" role="dialog" aria-modal="true">
  <div class="modal-header">
    <div class="modal-title">Add Property</div>
    <button class="modal-close" id="modal-close" type="button" aria-label="Close">&times;</button>
  </div>
  <div class="modal-body">
    <div class="modal-error" id="modal-error" style="display:none"></div>
    <div class="field-row full">
      <div class="field-group">
        <label for="np-addr1">Address Line 1 <span class="req">*</span></label>
        <input type="text" id="np-addr1" placeholder="123 Main St" />
      </div>
    </div>
    <div class="field-row full">
      <div class="field-group">
        <label for="np-addr2">Address Line 2</label>
        <input type="text" id="np-addr2" placeholder="Apt 2B, Unit 4…" />
      </div>
    </div>
    <div class="field-row">
      <div class="field-group">
        <label for="np-city">City <span class="req">*</span></label>
        <input type="text" id="np-city" placeholder="Abilene" />
      </div>
      <div class="field-group">
        <label for="np-zip">Zip Code <span class="req">*</span></label>
        <input type="text" id="np-zip" placeholder="79601" />
      </div>
    </div>
    <div class="field-row full">
      <div class="field-group">
        <label for="np-neighborhood">Neighborhood</label>
        <input type="text" id="np-neighborhood" placeholder="Optional" />
      </div>
    </div>
    <div class="field-row">
      <div class="field-group">
        <label for="np-bedrooms">Bedrooms</label>
        <select id="np-bedrooms">
          <option value="">—</option>
          <option value="1">1 BR</option>
          <option value="2">2 BR</option>
          <option value="3">3 BR</option>
          <option value="4">4 BR</option>
          <option value="5">5+ BR</option>
        </select>
      </div>
      <div class="field-group">
        <label for="np-bathrooms">Bathrooms</label>
        <select id="np-bathrooms">
          <option value="">—</option>
          <option value="1">1 BA</option>
          <option value="1.5">1.5 BA</option>
          <option value="2">2 BA</option>
          <option value="2.5">2.5 BA</option>
          <option value="3">3 BA</option>
          <option value="3.5">3.5 BA</option>
          <option value="4">4 BA</option>
        </select>
      </div>
    </div>
    <div class="field-row">
      <div class="field-group">
        <label for="np-type">Property Type</label>
        <select id="np-type">
          <option value="">—</option>
          <option value="House">House</option>
          <option value="Apartment">Apartment</option>
          <option value="Condo">Condo</option>
          <option value="Townhouse">Townhouse</option>
          <option value="Duplex">Duplex</option>
        </select>
      </div>
      <div class="field-group"></div>
    </div>
    <div class="field-row full">
      <div class="field-group">
        <label for="np-landlord">Landlord <span class="req">*</span></label>
        <select id="np-landlord">
          <option value="">— Select landlord —</option>
        </select>
      </div>
    </div>
    <div id="no-landlords-msg" class="assoc-banner warn" style="display:none">
      No landlords yet. <a href="/admin/landlords">Create one first →</a>
    </div>

    <!-- Quick-create landlord toggle -->
    <div class="quick-landlord-toggle">
      <button type="button" id="btn-toggle-ql" class="link-btn">
        <svg viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
        Quick-create landlord
      </button>
    </div>
    <div id="quick-landlord-form" class="quick-landlord-form" style="display:none">
      <div class="ql-title">New Landlord</div>
      <div class="field-row full">
        <div class="field-group">
          <label for="ql-name">Name <span class="req">*</span></label>
          <input type="text" id="ql-name" placeholder="Acme Property Mgmt" />
        </div>
      </div>
      <div class="field-row">
        <div class="field-group">
          <label for="ql-phone">Phone <span class="req">*</span></label>
          <input type="text" id="ql-phone" placeholder="(555) 000-0000" />
        </div>
        <div class="field-group">
          <label for="ql-email">Email <span class="req">*</span></label>
          <input type="email" id="ql-email" placeholder="contact@example.com" />
        </div>
      </div>
      <div class="field-row full">
        <div class="field-group">
          <label for="ql-type">Type <span class="req">*</span></label>
          <select id="ql-type">
            <option value="">— Select type —</option>
            <option>Property Management Company</option>
            <option>Individual Investor's LLC</option>
            <option>Small Local RE Company</option>
            <option>Regional Portfolio Operator</option>
            <option>Institutional Landlord</option>
            <option>Real Estate Agent</option>
            <option>Turnkey Rental Company</option>
            <option>Relocation/Corporate Housing Company</option>
          </select>
        </div>
      </div>
      <button type="button" class="btn btn-ghost btn-sm" id="btn-create-landlord">Create Landlord</button>
    </div>

    <div class="field-row full" style="margin-top:12px">
      <div class="field-group">
        <label for="np-notes">Notes</label>
        <textarea id="np-notes" placeholder="Optional"></textarea>
      </div>
    </div>

    <div class="modal-actions">
      <button type="button" class="btn btn-ghost" id="btn-modal-cancel">Cancel</button>
      <button type="button" class="btn btn-primary" id="btn-modal-save">Create Property</button>
    </div>
  </div>
</div>

<script>
  import { fmt } from '@lib/formatters';
  import { calcAll, calcMaintenance, getRiskFlags } from '@lib/calculations';
  import type { AnalysisInputs, RampSettings } from '@lib/calculations';
  import { OCC_CAP, PAYBACK_RULE_MONTHS } from '@lib/constants';
  import { supabase } from '@lib/supabase';
  import { requireAuth } from '@lib/auth';
  import { renderVisuals } from '@lib/charts';
  import type { RenderContext } from '@lib/charts';
  import { exportMarkdown } from '@lib/exportMarkdown';
  import { initPropertyModal } from '@lib/propertyModal';

  // ── DOM helpers ──────────────────────────────────────────────────────────

  function setOut(id: string, text: string, cls?: string) {
    const el = document.getElementById(id);
    if (!el) return;
    el.textContent = text;
    el.className = 'metric-value' + (cls ? ' ' + cls : '');
  }

  function inp(id: string): number {
    const el = document.getElementById(id) as HTMLInputElement | null;
    return el ? parseFloat(el.value) || 0 : 0;
  }

  function inpStr(id: string): string {
    const el = document.getElementById(id) as HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement | null;
    return el ? el.value.trim() : '';
  }

  function esc(str: string): string {
    return String(str)
      .replace(/&/g, '&amp;').replace(/</g, '&lt;')
      .replace(/>/g, '&gt;').replace(/"/g, '&quot;');
  }

  function show(id: string) { const el = document.getElementById(id); if (el) el.style.display = ''; }
  function hide(id: string) { const el = document.getElementById(id); if (el) el.style.display = 'none'; }

  // ── State ─────────────────────────────────────────────────────────────────

  let currentAnalysisId: string | null = null;
  let selectedPropertyId: string | null = null;
  let propertiesList: any[] = [];
  let lastResults: ReturnType<typeof calcAll> | null = null;

  // ── Accordion ────────────────────────────────────────────────────────────

  document.querySelectorAll<HTMLButtonElement>('.calc-section-toggle').forEach(btn => {
    btn.addEventListener('click', () => {
      const id   = btn.dataset.target!;
      const body = document.getElementById(id)!;
      const open = body.classList.contains('open');
      body.classList.toggle('open', !open);
      btn.classList.toggle('open', !open);
    });
  });

  // ── Calculations ─────────────────────────────────────────────────────────

  function gatherInputs(): AnalysisInputs {
    return {
      rentNeg:              inp('inp-rent-neg'),
      rentAsk:              inp('inp-rent-ask'),
      deposit:              inp('inp-deposit'),
      adr:                  inp('inp-adr'),
      occ:                  inp('inp-occ'),
      avgStay:              inp('inp-stay'),
      cleaning:             inp('inp-cleaning'),
      utilities:            0,
      electricity:          inp('inp-electricity'),
      water:                inp('inp-water'),
      sewer:                inp('inp-sewer'),
      garbage:              inp('inp-garbage'),
      internet:             inp('inp-internet'),
      insurance:            inp('inp-insurance'),
      supplies:             inp('inp-supplies'),
      linens:               inp('inp-linens'),
      pms:                  inp('inp-pms'),
      pricing:              inp('inp-pricing'),
      minutSubscription:    inp('inp-minut-subscription'),
      streaming:            inp('inp-streaming'),
      airbnbFeeType:        (inpStr('inp-airbnb-fee-type') as '3%' | '15.5%') || '3%',
      hasYard:              inpStr('inp-has-yard') === 'Yes',
      lawnCare:             inp('inp-lawn-care'),
      pestControl:          inp('inp-pest-control'),
      bulkPickup:           inp('inp-bulk-pickup'),
      preventiveInspection: inp('inp-preventive-inspection'),
      hvacFilters:          inp('inp-hvac-filters'),
      cpa:                  inp('inp-cpa'),
      furniture:            inp('inp-furniture'),
      photo:                inp('inp-photo'),
      lock:                 inp('inp-lock'),
      legal:                inp('inp-legal'),
      misc:                 inp('inp-misc'),
      minutHardware:        inp('inp-minut-hardware'),
      wifiRouter:           inp('inp-wifi-router'),
      welcomeKits:          inp('inp-welcome-kits'),
      isHOA:                inpStr('inp-hoa') === 'Yes',
    };
  }

  function gatherRamp(): RampSettings {
    return {
      enabled:           inpStr('inp-ramp-enabled') === 'Yes',
      makeReadyMonth:    inpStr('inp-make-ready') === 'Yes',
      rentFreeMonth1:    inpStr('inp-rent-free') === 'Yes',
      paybackRuleMonths: inp('inp-payback-rule') || PAYBACK_RULE_MONTHS,
      phases: [
        { months: inp('inp-ramp1-months'), occFactor: inp('inp-ramp1-occ'), adrFactor: inp('inp-ramp1-adr') },
        { months: inp('inp-ramp2-months'), occFactor: inp('inp-ramp2-occ'), adrFactor: inp('inp-ramp2-adr') },
        { months: Infinity, occFactor: 1, adrFactor: 1 },
      ],
    };
  }

  function runCalc() {
    const inputs = gatherInputs();
    const ramp   = gatherRamp();
    const isHOA  = inputs.isHOA;

    // Occupancy > 95% is clamped in the engine; show the inline note here.
    const capNote = document.getElementById('occ-cap-note');
    if (capNote) capNote.style.display = inputs.occ > OCC_CAP ? '' : 'none';

    const rampFields = document.getElementById('ramp-fields');
    if (rampFields) rampFields.style.display = ramp.enabled ? '' : 'none';

    document.getElementById('hoa-block')?.classList.toggle('visible', isHOA);
    document.getElementById('hoa-save-block')?.classList.toggle('visible', isHOA);

    const saveBtn   = document.getElementById('btn-save')   as HTMLButtonElement;
    const updateBtn = document.getElementById('btn-update') as HTMLButtonElement;
    const exportBtn = document.getElementById('btn-export') as HTMLButtonElement;
    saveBtn.disabled   = isHOA || !selectedPropertyId;
    updateBtn.disabled = isHOA;
    exportBtn.disabled = isHOA;

    const maintenance = calcMaintenance(inputs.rentNeg);
    const maintEl = document.getElementById('inp-maintenance') as HTMLInputElement;
    if (maintEl) maintEl.value = maintenance.toFixed(2);

    const r = calcAll(inputs, ramp);
    lastResults = r;

    const staysPerMonth = r.staysPerMonth;
    const cleaningTotal = staysPerMonth * inputs.cleaning;
    setOut('out-stays',              staysPerMonth.toFixed(1));
    setOut('out-cleaning-collected', fmt(cleaningTotal));
    setOut('out-cleaning-paid',      fmt(cleaningTotal));

    const flags  = getRiskFlags(inputs, r, ramp.paybackRuleMonths);
    const riskEl = document.getElementById('risk-flags')!;
    riskEl.innerHTML = flags.length === 0
      ? '<div class="risk-flag ok"><span class="risk-flag-icon">✓</span>No risk flags triggered.</div>'
      : flags.map(f => `<div class="risk-flag ${f.type}"><span class="risk-flag-icon">${f.icon}</span>${f.text}</div>`).join('');

    const ctx: RenderContext = {
      propertiesList,
      selectedPropertyId,
      walkAway: inp('inp-walk-away'),
      driver:   inpStr('inp-demand'),
      address:  inpStr('inp-address'),
      bedrooms: inpStr('inp-bedrooms'),
      bathrooms: inpStr('inp-bathrooms'),
      propertyType: inpStr('inp-type'),
      isHOA,
      ramp,
    };
    renderVisuals(inputs, r, ctx);
  }

  const inputIds = [
    'inp-name', 'inp-address',
    'inp-rent-ask', 'inp-rent-neg', 'inp-deposit', 'inp-hoa', 'inp-walk-away',
    'inp-adr', 'inp-occ', 'inp-stay', 'inp-demand',
    'inp-ramp-enabled', 'inp-payback-rule', 'inp-make-ready', 'inp-rent-free',
    'inp-ramp1-months', 'inp-ramp1-occ', 'inp-ramp1-adr',
    'inp-ramp2-months', 'inp-ramp2-occ', 'inp-ramp2-adr',
    'inp-electricity', 'inp-water', 'inp-sewer', 'inp-garbage',
    'inp-internet', 'inp-insurance',
    'inp-supplies', 'inp-linens',
    'inp-pms', 'inp-pricing', 'inp-minut-subscription', 'inp-streaming', 'inp-airbnb-fee-type',
    'inp-has-yard', 'inp-lawn-care', 'inp-pest-control', 'inp-bulk-pickup',
    'inp-preventive-inspection', 'inp-hvac-filters', 'inp-cpa',
    'inp-cleaning',
    'inp-furniture', 'inp-photo', 'inp-lock', 'inp-legal', 'inp-misc',
    'inp-minut-hardware', 'inp-wifi-router', 'inp-welcome-kits',
    'inp-notes',
  ];
  inputIds.forEach(id => {
    document.getElementById(id)?.addEventListener('input', runCalc);
  });

  // ── Has Yard toggle ───────────────────────────────────────────────────────────

  // Keeps the lawn-care field's editability in sync with Has Yard. Must run
  // whenever inp-has-yard changes programmatically too (loading a saved
  // analysis, reset), not just on user interaction.
  function syncLawnField(setDefault: boolean) {
    const hasYard = inpStr('inp-has-yard') === 'Yes';
    const lawnEl = document.getElementById('inp-lawn-care') as HTMLInputElement;
    if (!lawnEl) return;
    lawnEl.readOnly = !hasYard;
    if (!hasYard) lawnEl.value = '0';
    else if (setDefault) lawnEl.value = '78';
  }

  document.getElementById('inp-has-yard')?.addEventListener('change', () => {
    syncLawnField(true);
    runCalc();
  });

  // ── Property picker ──────────────────────────────────────────────────────────

  async function loadProperties() {
    const { data, error } = await supabase
      .from('properties')
      .select('*, landlord:landlords(id, name)')
      .order('created_at', { ascending: false });

    propertiesList = data || [];
    const select = document.getElementById('inp-property') as HTMLSelectElement;

    if (error || !propertiesList.length) {
      select.innerHTML = '<option value="">— No properties yet —</option>';
      show('no-properties-hint');
      return;
    }

    hide('no-properties-hint');
    select.innerHTML = '<option value="">— Select a property —</option>' +
      propertiesList.map(p =>
        `<option value="${esc(p.id)}">${esc(p.address_line1)}, ${esc(p.city)}</option>`
      ).join('');

    if (selectedPropertyId) {
      select.value = selectedPropertyId;
    }
  }

  function applyPropertyMeta(prop: any | null) {
    const bedroomsEl  = document.getElementById('inp-bedrooms')  as HTMLInputElement;
    const bathroomsEl = document.getElementById('inp-bathrooms') as HTMLInputElement;
    const typeEl      = document.getElementById('inp-type')      as HTMLInputElement;
    if (!bedroomsEl || !bathroomsEl || !typeEl) return;
    if (prop) {
      bedroomsEl.value  = prop.bedrooms      != null ? String(prop.bedrooms)      : '';
      bathroomsEl.value = prop.bathrooms     != null ? String(prop.bathrooms)     : '';
      typeEl.value      = prop.property_type != null ? String(prop.property_type) : '';
    } else {
      bedroomsEl.value  = '';
      bathroomsEl.value = '';
      typeEl.value      = '';
    }
  }

  document.getElementById('inp-property')?.addEventListener('change', e => {
    const val = (e.target as HTMLSelectElement).value;
    selectedPropertyId = val || null;

    const prop = propertiesList.find(p => p.id === val);
    const addrEl = document.getElementById('inp-address') as HTMLInputElement;

    if (prop && addrEl) {
      const parts = [prop.address_line1];
      if (prop.address_line2) parts.push(prop.address_line2);
      parts.push(prop.city);
      addrEl.value = parts.join(', ');
    } else if (addrEl) {
      addrEl.value = '';
    }

    // Default label to address_line1 only if still showing the default placeholder
    const nameEl = document.getElementById('inp-name') as HTMLInputElement;
    if (prop && nameEl && nameEl.value.trim() === 'New Analysis') {
      nameEl.value = prop.address_line1;
    }

    applyPropertyMeta(prop ?? null);

    hide('orphan-warning');
    if (!selectedPropertyId) {
      show('no-property-hint');
    } else {
      hide('no-property-hint');
    }

    runCalc();
  });

  // ── Persistence ──────────────────────────────────────────────────────────────

  function gatherFormInputs() {
    const obj: Record<string, string> = {};
    inputIds.forEach(id => {
      const el = document.getElementById(id) as HTMLInputElement | null;
      if (el) obj[id] = el.value;
    });
    return obj;
  }

  function applyFormInputs(inputs: Record<string, string>) {
    // Analyses saved before ramp mode existed carry no ramp fields: default
    // them to ramp OFF so their stored numbers still reproduce.
    if (!('inp-ramp-enabled' in inputs)) {
      const rampEl = document.getElementById('inp-ramp-enabled') as HTMLSelectElement | null;
      if (rampEl) rampEl.value = 'No';
    }
    Object.entries(inputs).forEach(([id, val]) => {
      const el = document.getElementById(id) as HTMLInputElement | null;
      if (el && !el.disabled && !el.readOnly) el.value = val;
    });
    // Lawn care is readOnly while Has Yard is No, so the loop above skipped
    // it. Sync its editability to the loaded Has Yard value, then restore the
    // saved amount.
    syncLawnField(false);
    if (inpStr('inp-has-yard') === 'Yes' && 'inp-lawn-care' in inputs) {
      const lawnEl = document.getElementById('inp-lawn-care') as HTMLInputElement | null;
      if (lawnEl) lawnEl.value = inputs['inp-lawn-care'];
    }
    runCalc();
  }

  function buildResults() {
    if (!lastResults) return {};
    const r = lastResults;
    return {
      grossRevenue: r.grossRevenue, airbnbFee: r.airbnbFee, netPlatform: r.netPlatform,
      fixed: r.fixed, maintenance: r.maintenance, staysPerMonth: r.staysPerMonth,
      netMonthly: r.netMonthly, netAnnual: r.netAnnual, breakEvenOcc: r.breakEvenOcc,
      margin: r.margin, multiple: r.multiple, totalInvest: r.totalInvest,
      payback: isFinite(r.payback) ? r.payback : null,
      roi12: r.roi12, roi24: r.roi24,
      rampEnabled: r.rampEnabled, month1Carry: r.month1Carry,
      paybackRamped: r.paybackRamped != null && isFinite(r.paybackRamped) ? r.paybackRamped : null,
    };
  }

  async function loadAnalysisById(id: string) {
    const { data, error } = await supabase
      .from('property_analyses')
      .select('*')
      .eq('id', id)
      .single();
    if (error || !data) return;

    currentAnalysisId = id;
    selectedPropertyId = data.property_id || null;
    applyFormInputs(data.inputs);

    const propSelect = document.getElementById('inp-property') as HTMLSelectElement;
    if (propSelect) propSelect.value = selectedPropertyId ?? '';

    const addrEl = document.getElementById('inp-address') as HTMLInputElement;
    if (addrEl && data.address) addrEl.value = data.address;

    applyPropertyMeta(propertiesList.find(p => p.id === selectedPropertyId) ?? null);

    if (!data.property_id) {
      show('orphan-warning');
      hide('no-property-hint');
    } else {
      hide('orphan-warning');
      hide('no-property-hint');
    }

    (document.getElementById('btn-save')   as HTMLElement).style.display = 'none';
    (document.getElementById('btn-update') as HTMLElement).style.display = '';
    (document.getElementById('btn-new')    as HTMLElement).style.display = '';
    history.replaceState(null, '', '/admin/analyzer');
    runCalc();
  }

  async function saveAnalysis() {
    if (!selectedPropertyId) return;
    const btn = document.getElementById('btn-save') as HTMLButtonElement;
    btn.disabled = true; btn.textContent = 'Saving…';

    const prop = propertiesList.find(p => p.id === selectedPropertyId);
    const address = prop
      ? [prop.address_line1, prop.address_line2, prop.city].filter(Boolean).join(', ')
      : inpStr('inp-address');

    const { data, error } = await supabase
      .from('property_analyses')
      .insert([{
        name:        inpStr('inp-name') || 'New Analysis',
        address,
        property_id: selectedPropertyId,
        inputs:      gatherFormInputs(),
        results:     buildResults(),
        notes:       inpStr('inp-notes'),
      }])
      .select().single();

    btn.disabled = false;
    btn.innerHTML = `<svg viewBox="0 0 24 24"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg> Save Analysis`;

    if (error) { alert('Save failed: ' + error.message); return; }
    currentAnalysisId = data.id;
    (document.getElementById('btn-save')   as HTMLElement).style.display = 'none';
    (document.getElementById('btn-update') as HTMLElement).style.display = '';
    (document.getElementById('btn-new')    as HTMLElement).style.display = '';
  }

  async function updateAnalysis() {
    if (!currentAnalysisId) return;
    const btn = document.getElementById('btn-update') as HTMLButtonElement;
    btn.disabled = true; btn.textContent = 'Updating…';

    const prop = propertiesList.find(p => p.id === selectedPropertyId);
    const address = prop
      ? [prop.address_line1, prop.address_line2, prop.city].filter(Boolean).join(', ')
      : inpStr('inp-address');

    const { error } = await supabase
      .from('property_analyses')
      .update({
        name:        inpStr('inp-name') || 'New Analysis',
        address,
        property_id: selectedPropertyId,
        inputs:      gatherFormInputs(),
        results:     buildResults(),
        notes:       inpStr('inp-notes'),
        updated_at:  new Date().toISOString(),
      })
      .eq('id', currentAnalysisId);

    btn.disabled = false;
    btn.innerHTML = `<svg viewBox="0 0 24 24"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg> Update`;
    if (error) alert('Update failed: ' + error.message);
  }

  function resetForm() {
    currentAnalysisId = null;
    selectedPropertyId = null;
    hide('orphan-warning');
    hide('no-property-hint');

    const defaults: Record<string, string> = {
      'inp-name': 'New Analysis', 'inp-address': '',
      'inp-rent-ask': '1400', 'inp-rent-neg': '1200', 'inp-deposit': '1400', 'inp-hoa': 'No',
      'inp-adr': '167', 'inp-occ': '77', 'inp-stay': '3.2', 'inp-demand': 'Military TDY',
      'inp-walk-away': '1750',
      'inp-ramp-enabled': 'Yes', 'inp-payback-rule': '9', 'inp-make-ready': 'Yes', 'inp-rent-free': 'Yes',
      'inp-ramp1-months': '3', 'inp-ramp1-occ': '0.85', 'inp-ramp1-adr': '0.90',
      'inp-ramp2-months': '3', 'inp-ramp2-occ': '0.93', 'inp-ramp2-adr': '0.96',
      'inp-electricity': '130', 'inp-water': '45', 'inp-sewer': '50', 'inp-garbage': '24',
      'inp-internet': '80', 'inp-insurance': '100',
      'inp-supplies': '120', 'inp-linens': '41',
      'inp-pms': '39', 'inp-pricing': '20',
      'inp-minut-subscription': '10', 'inp-streaming': '8', 'inp-airbnb-fee-type': '3%',
      'inp-has-yard': 'No', 'inp-lawn-care': '0', 'inp-pest-control': '51', 'inp-bulk-pickup': '8',
      'inp-preventive-inspection': '50', 'inp-hvac-filters': '10', 'inp-cpa': '42',
      'inp-cleaning': '120',
      'inp-furniture': '6500', 'inp-photo': '205', 'inp-lock': '180', 'inp-legal': '400',
      'inp-misc': '500', 'inp-minut-hardware': '100', 'inp-wifi-router': '100', 'inp-welcome-kits': '195',
      'inp-notes': '',
    };
    Object.entries(defaults).forEach(([id, val]) => {
      const el = document.getElementById(id) as HTMLInputElement | null;
      if (el) el.value = val;
    });
    syncLawnField(false);
    const propSelect = document.getElementById('inp-property') as HTMLSelectElement;
    if (propSelect) propSelect.value = '';

    applyPropertyMeta(null);

    (document.getElementById('btn-save')   as HTMLElement).style.display = '';
    (document.getElementById('btn-update') as HTMLElement).style.display = 'none';
    (document.getElementById('btn-new')    as HTMLElement).style.display = 'none';
    show('no-property-hint');
    runCalc();
  }

  // ── Button wiring ────────────────────────────────────────────────────────────

  document.getElementById('btn-save')!.addEventListener('click', saveAnalysis);
  document.getElementById('btn-update')!.addEventListener('click', updateAnalysis);
  document.getElementById('btn-export')!.addEventListener('click', () => {
    if (!lastResults) return;
    const inputs = gatherInputs();
    const ramp   = gatherRamp();
    exportMarkdown(inputs, lastResults, {
      name:         inpStr('inp-name'),
      address:      inpStr('inp-address'),
      bedrooms:     inpStr('inp-bedrooms'),
      bathrooms:    inpStr('inp-bathrooms'),
      propertyType: inpStr('inp-type'),
      notes:        inpStr('inp-notes'),
      riskFlags:    getRiskFlags(inputs, lastResults, ramp.paybackRuleMonths),
    }, ramp);
  });
  document.getElementById('btn-new')!.addEventListener('click', () => {
    resetForm();
    loadProperties();
  });

  // ── New Property Modal ────────────────────────────────────────────────────────

  initPropertyModal({
    onPropertyCreated(property) {
      propertiesList.unshift(property);
      const select = document.getElementById('inp-property') as HTMLSelectElement;
      hide('no-properties-hint');
      select.innerHTML = '<option value="">— Select a property —</option>' +
        propertiesList.map(p =>
          `<option value="${esc(p.id)}">${esc(p.address_line1)}, ${esc(p.city)}</option>`
        ).join('');
      select.value = property.id;
      selectedPropertyId = property.id;
      const addrEl = document.getElementById('inp-address') as HTMLInputElement;
      if (addrEl) {
        const parts = [property.address_line1];
        if (property.address_line2) parts.push(property.address_line2);
        parts.push(property.city);
        addrEl.value = parts.join(', ');
      }
      applyPropertyMeta(property);
      hide('orphan-warning');
      hide('no-property-hint');
      runCalc();
    },
  });

  // ── Init ─────────────────────────────────────────────────────────────────────

  (async () => {
    const session = await requireAuth();
    if (!session) return;

    runCalc();
    await loadProperties();

    // Check for ?load= URL param (navigated from Saved Analyses)
    const loadId = new URLSearchParams(location.search).get('load');
    if (loadId) {
      await loadAnalysisById(loadId);
    } else {
      // Check for property pre-selection from Properties page "New Analysis" shortcut
      const preselectedId   = sessionStorage.getItem('analyzer_property_id');
      const preselectedAddr = sessionStorage.getItem('analyzer_property_addr');
      if (preselectedId) {
        sessionStorage.removeItem('analyzer_property_id');
        sessionStorage.removeItem('analyzer_property_addr');
        selectedPropertyId = preselectedId;
        const propSelect = document.getElementById('inp-property') as HTMLSelectElement;
        if (propSelect) propSelect.value = preselectedId;
        const addrEl = document.getElementById('inp-address') as HTMLInputElement;
        if (addrEl && preselectedAddr) addrEl.value = preselectedAddr;
        applyPropertyMeta(propertiesList.find(p => p.id === preselectedId) ?? null);
        hide('orphan-warning');
        hide('no-property-hint');
        runCalc();
      } else {
        show('no-property-hint');
      }
    }

    document.body.classList.add('ready');
  })();
</script>

<style>
  body { font-family: 'DM Sans', system-ui, sans-serif; background: #f6f5f3; color: #1a3327; font-size: 16px; line-height: 1.6; min-height: 100vh; overflow-x: hidden; opacity: 0; transition: opacity .4s ease; font-optical-sizing: auto; }
  body.ready { opacity: 1; }

  .page { min-height: 100vh; position: relative; z-index: 1; }
  .portal-content { padding: 48px 20px 80px; max-width: 1500px; margin: 0 auto; }
  .section-label { font-family: 'DM Sans', system-ui, sans-serif; font-size: .65rem; font-weight: 500; letter-spacing: .22em; text-transform: uppercase; color: #a8885a; display: flex; align-items: center; gap: 12px; margin-bottom: 32px; }
  .section-label::before { content: ''; display: block; width: 28px; height: 1px; background: #a8885a; }

  .calc-layout { display: grid; grid-template-columns: 460px 1fr; gap: 16px; align-items: start; }
  .calc-panel { background: #ffffff; border: 1px solid rgba(26,51,39,.1); box-shadow: 0 1px 4px rgba(26,51,39,.06); padding: 32px 28px; position: sticky; top: 20px; max-height: calc(100vh - 40px); overflow-y: auto; }
  .calc-panel-title { font-family: 'Cormorant Garamond', Georgia, serif; font-size: 1.15rem; font-weight: 500; color: #1a3327; margin-bottom: 24px; letter-spacing: .01em; padding-bottom: 14px; border-bottom: 1px solid rgba(26,51,39,.08); display: flex; justify-content: space-between; align-items: center; }

  /* Property Association */
  .property-assoc { padding: 0 0 18px; margin-bottom: 4px; border-bottom: 1px solid rgba(26,51,39,.08); }
  .property-assoc-row { display: flex; align-items: flex-end; gap: 10px; margin-bottom: 8px; }
  .btn-add-prop {
    display: inline-flex; align-items: center; gap: 5px; flex-shrink: 0;
    padding: 9px 14px; border: 1px solid rgba(26,51,39,.2); border-radius: 2px;
    font-family: 'DM Sans', system-ui, sans-serif; font-size: .72rem; font-weight: 400;
    letter-spacing: .08em; text-transform: uppercase; color: rgba(26,51,39,.6);
    background: none; cursor: pointer; transition: border-color .2s, color .2s, background .2s;
    white-space: nowrap;
  }
  .btn-add-prop:hover { border-color: #c9a96e; color: #a8885a; }
  .btn-add-prop svg { width: 11px; height: 11px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; }
  .assoc-banner {
    font-size: .75rem; padding: 8px 12px; border-radius: 2px; line-height: 1.5; margin-top: 6px;
  }
  .assoc-banner a { color: inherit; font-weight: 500; text-decoration: underline; }
  .assoc-banner.warn { background: rgba(212,163,42,.1); border: 1px solid rgba(212,163,42,.3); color: rgba(180,130,0,.9); }
  .assoc-banner.info { background: rgba(201,169,110,.07); border: 1px solid rgba(201,169,110,.2); color: rgba(26,51,39,.55); }

  .calc-section { border-bottom: 1px solid rgba(26,51,39,.08); }
  .calc-section:last-child { border-bottom: none; }
  .calc-section-toggle { width: 100%; display: flex; align-items: center; justify-content: space-between; padding: 14px 0; background: transparent; border: none; cursor: pointer; font-family: 'DM Sans', system-ui, sans-serif; font-size: .72rem; font-weight: 500; letter-spacing: .14em; text-transform: uppercase; color: #a8885a; transition: color .35s; }
  .calc-section-toggle:hover { color: #c9a96e; }
  .calc-section-toggle svg { width: 14px; height: 14px; stroke: currentColor; fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; transition: transform .25s ease; flex-shrink: 0; }
  .calc-section-toggle.open svg { transform: rotate(180deg); }
  .calc-section-body { display: none; padding-bottom: 16px; }
  .calc-section-body.open { display: block; }
  .calc-subsection-title { font-size: .64rem; font-weight: 500; letter-spacing: .16em; text-transform: uppercase; color: rgba(26,51,39,.35); padding: 12px 0 4px; border-top: 1px solid rgba(26,51,39,.06); margin-top: 4px; }
  .calc-subsection-title:first-child { border-top: none; margin-top: 0; padding-top: 6px; }

  .field-row { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 10px; }
  .field-row.full { grid-template-columns: 1fr; }
  .field-group { display: flex; flex-direction: column; gap: 4px; }
  label { font-size: .68rem; letter-spacing: .1em; text-transform: uppercase; color: #7a6e65; font-weight: 400; }
  .req { color: #c94a4a; font-style: normal; }
  input[type="text"], input[type="number"], input[type="email"], select, textarea { background: #ffffff; border: 1px solid rgba(26,51,39,.15); border-radius: 2px; padding: 9px 12px; font-family: 'DM Sans', system-ui, sans-serif; font-size: .88rem; color: #1a3327; width: 100%; transition: border-color .35s, background .35s; -webkit-appearance: none; appearance: none; }
  input:focus, select:focus, textarea:focus { outline: none; border-color: #c9a96e; background: rgba(201,169,110,.04); }
  input[readonly], input[disabled] { opacity: .5; cursor: not-allowed; background: rgba(26,51,39,.03); }
  input.prop-auto-field { opacity: 1; cursor: default; color: rgba(26,51,39,.6); font-style: italic; }
  input::placeholder, textarea::placeholder { color: #8a7e78; }
  select option { background: #ffffff; color: #1a3327; }
  textarea { resize: vertical; min-height: 72px; line-height: 1.5; }
  .field-note { font-size: .72rem; color: #7a6e65; line-height: 1.4; font-style: italic; margin-top: 2px; }
  .occ-cap-note { color: #b07a10; }
  .ramp-phase-row { grid-template-columns: 1fr 1fr 1fr; }
  .field-badge { display: inline-flex; align-items: center; gap: 6px; padding: 3px 8px; border: 1px solid rgba(201,169,110,.2); border-radius: 2px; font-size: .65rem; letter-spacing: .1em; text-transform: uppercase; color: rgba(201,169,110,.6); font-style: normal; }

  .hoa-block { display: none; background: rgba(201,74,74,.12); border: 2px solid #c94a4a; border-radius: 2px; padding: 16px 18px; margin-top: 12px; }
  .hoa-block.visible { display: block; }
  .hoa-block-title { font-family: 'DM Sans', system-ui, sans-serif; font-size: .8rem; font-weight: 500; letter-spacing: .08em; text-transform: uppercase; color: #c94a4a; margin-bottom: 6px; display: flex; align-items: center; gap: 8px; }
  .hoa-block-body { font-size: .88rem; color: rgba(201,74,74,.9); line-height: 1.6; }
  .hot-info { background: rgba(201,169,110,.05); border: 1px solid rgba(201,169,110,.15); border-radius: 2px; padding: 14px 16px; margin-top: 8px; }
  .hot-info p { font-size: .88rem; color: #7a6e65; line-height: 1.65; }
  .hot-info strong { color: rgba(26,51,39,.7); font-weight: 400; }

  .output-panel { background: #ffffff; border: 1px solid rgba(26,51,39,.1); box-shadow: 0 1px 4px rgba(26,51,39,.06); padding: 24px 22px; display: flex; flex-direction: column; gap: 18px; }
  .out-section-title { font-family: 'DM Sans', system-ui, sans-serif; font-size: .65rem; font-weight: 500; letter-spacing: .18em; text-transform: uppercase; color: #a8885a; margin-bottom: 14px; padding-bottom: 8px; border-bottom: 1px solid rgba(201,169,110,.08); }
  .metric-row { display: flex; justify-content: space-between; align-items: baseline; padding: 5px 0; border-bottom: 1px solid rgba(26,51,39,.06); font-size: .83rem; gap: 8px; }
  .metric-row:last-child { border-bottom: none; }
  .metric-label { color: #7a6e65; font-weight: 300; flex-shrink: 0; }
  .metric-value { font-weight: 400; letter-spacing: .02em; text-align: right; color: #1a3327; }
  .metric-value.green { color: #4caf81; }
  .metric-value.amber { color: #d4a32a; }
  .metric-value.red   { color: #c94a4a; }
  .metric-row.highlight { background: rgba(26,51,39,.04); margin: 0 -8px; padding: 7px 8px; border-bottom: 1px solid rgba(26,51,39,.06); }
  .metric-row.highlight .metric-label { color: rgba(26,51,39,.65); font-weight: 400; }
  .metric-row.highlight .metric-value { font-size: .95rem; }
  .metric-row--gross { padding-bottom: 10px; }
  .cost-stack-label { font-size: .62rem; letter-spacing: .14em; text-transform: uppercase; color: #a8885a; font-weight: 500; padding: 8px 0 4px; border-top: 1px solid rgba(201,169,110,.12); margin-top: 4px; }
  .metric-row.cost-item { padding: 4px 0 4px 10px; font-size: .8rem; border-bottom: 1px solid rgba(26,51,39,.04); }
  .metric-row.cost-item .metric-label { color: rgba(26,51,39,.55); font-weight: 300; }
  .metric-row--total { border-top: 1px solid rgba(26,51,39,.12); margin-top: 2px; padding-top: 7px; font-weight: 500; }
  .metric-row--total .metric-label { color: rgba(26,51,39,.75); font-weight: 500; }
  .passthrough-note { font-size: .75rem; color: #7a6e65; font-style: italic; margin-bottom: 10px; line-height: 1.5; }

  .scenario-table { width: 100%; border-collapse: collapse; font-size: .8rem; margin-top: 8px; }
  .scenario-table th { font-size: .68rem; letter-spacing: .12em; text-transform: uppercase; color: #7a6e65; font-weight: 400; padding: 12px 16px; text-align: right; border-bottom: 1px solid rgba(26,51,39,.08); }
  .scenario-table th:first-child { text-align: left; }
  .scenario-table td { padding: 11px 16px; font-size: .92rem; color: #1a3327; border-bottom: 1px solid rgba(26,51,39,.08); text-align: right; }
  .scenario-table td:first-child { text-align: left; color: rgba(26,51,39,.5); font-size: .75rem; letter-spacing: .06em; text-transform: uppercase; }
  .scenario-table tr.worst-negative td { background: rgba(201,74,74,.08); color: #c94a4a; }
  .scenario-table tr.scenario-best td:not(:first-child) { color: #4caf81; }

  /* :global() required — these elements are created via innerHTML in JS and don't receive Astro's scope attribute */
  :global(.risk-flags) { display: flex; flex-direction: column; gap: 7px; }
  :global(.risk-flag) { display: flex; align-items: center; gap: 8px; padding: 7px 12px; border-radius: 2px; font-size: .72rem; line-height: 1.4; font-family: 'DM Sans', system-ui, sans-serif; }
  :global(.risk-flag.warn)   { background: rgba(212,163,42,.1); border: 1px solid rgba(212,163,42,.25); color: rgba(150,115,20,.9); }
  :global(.risk-flag.danger) { background: rgba(201,74,74,.1); border: 1px solid rgba(201,74,74,.3); color: rgba(180,55,55,.9); }
  :global(.risk-flag.ok)     { background: rgba(76,175,129,.08); border: 1px solid rgba(76,175,129,.18); color: rgba(29,130,95,.9); font-size: .68rem; }
  :global(.risk-flag.info)   { background: rgba(55,138,221,.07); border: 1px solid rgba(55,138,221,.2); color: rgba(35,95,160,.9); }
  :global(.risk-flag-icon) { flex-shrink: 0; font-size: .7rem; line-height: 1; }

  .calc-actions { display: flex; gap: 10px; flex-wrap: wrap; padding-top: 16px; border-top: 1px solid rgba(201,169,110,.1); }
  .btn { display: inline-flex; align-items: center; gap: 8px; padding: 10px 20px; border-radius: 2px; cursor: pointer; font-family: 'DM Sans', system-ui, sans-serif; font-size: .75rem; font-weight: 400; letter-spacing: .1em; text-transform: uppercase; transition: all .35s; border: 1px solid; }
  .btn-primary { background: #c9a96e; color: #1a3327; border-color: #c9a96e; }
  .btn-primary:hover { background: #dfc090; border-color: #dfc090; }
  .btn-primary:disabled { opacity: .4; cursor: not-allowed; }
  .btn-ghost { background: none; color: rgba(26,51,39,.7); border-color: rgba(26,51,39,.25); }
  .btn-ghost:hover { border-color: #1a3327; color: #1a3327; }
  .btn-ghost:disabled { opacity: .4; cursor: not-allowed; }
  .btn-sm { padding: 7px 14px; font-size: .7rem; }
  .btn svg { width: 13px; height: 13px; stroke: currentColor; fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }

  .view-saved-link { padding-top: 4px; text-align: center; }
  .view-saved-link a { font-size: .72rem; color: #a8885a; letter-spacing: .04em; text-decoration: none; transition: color .2s; }
  .view-saved-link a:hover { color: #c9a96e; }

  /* Modal */
  .modal-backdrop { position: fixed; inset: 0; background: rgba(26,51,39,.35); z-index: 200; }
  .modal {
    position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%);
    width: 480px; max-width: calc(100vw - 32px); max-height: 90vh; overflow-y: auto;
    background: #ffffff; border: 1px solid rgba(26,51,39,.12);
    box-shadow: 0 8px 40px rgba(26,51,39,.18); z-index: 201; border-radius: 3px;
  }
  .modal-header { display: flex; align-items: center; justify-content: space-between; padding: 22px 24px 16px; border-bottom: 1px solid rgba(26,51,39,.08); }
  .modal-title { font-family: 'Cormorant Garamond', Georgia, serif; font-size: 1.1rem; font-weight: 500; color: #1a3327; }
  .modal-close { background: none; border: none; cursor: pointer; font-size: 1.4rem; color: rgba(26,51,39,.4); line-height: 1; padding: 4px 8px; transition: color .2s; }
  .modal-close:hover { color: #1a3327; }
  .modal-body { padding: 20px 24px 24px; display: flex; flex-direction: column; gap: 12px; }
  .modal-error { background: rgba(201,74,74,.1); border: 1px solid rgba(201,74,74,.3); color: #c94a4a; padding: 10px 14px; border-radius: 2px; font-size: .82rem; }
  .modal-actions { display: flex; gap: 10px; justify-content: flex-end; padding-top: 8px; border-top: 1px solid rgba(26,51,39,.08); margin-top: 4px; }

  /* Quick-create landlord */
  .quick-landlord-toggle { margin-top: -4px; }
  .link-btn { background: none; border: none; cursor: pointer; display: inline-flex; align-items: center; gap: 5px; font-family: 'DM Sans', system-ui, sans-serif; font-size: .72rem; color: #a8885a; letter-spacing: .04em; padding: 0; transition: color .2s; }
  .link-btn:hover { color: #c9a96e; }
  .quick-landlord-form { background: rgba(201,169,110,.05); border: 1px solid rgba(201,169,110,.15); border-radius: 2px; padding: 16px; display: flex; flex-direction: column; gap: 10px; margin-top: 4px; }
  .ql-title { font-size: .65rem; letter-spacing: .16em; text-transform: uppercase; color: #a8885a; font-weight: 500; margin-bottom: 2px; }

  footer { border-top: 1px solid rgba(201,169,110,.1); padding: 24px 5vw; display: flex; align-items: center; justify-content: space-between; gap: 16px; flex-wrap: wrap; position: relative; z-index: 1; }
  .footer-brand { font-family: 'Cormorant Garamond', Georgia, serif; font-size: .95rem; color: rgba(26,51,39,.65); letter-spacing: .04em; }
  .footer-brand span { color: #a8885a; }
  .footer-meta { font-size: .72rem; color: #7a6e65; letter-spacing: .06em; }

  /* ── Visual results panel ──────────────────────────────────────────────── */
  .res-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 12px; padding-bottom: 14px; border-bottom: 1px solid rgba(26,51,39,.08); }
  .res-header-main { min-width: 0; }
  .res-address { font-family: 'DM Sans', system-ui, sans-serif; font-size: .95rem; font-weight: 500; color: #1a3327; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .res-subline { font-size: .72rem; color: #7a6e65; margin-top: 3px; letter-spacing: .02em; }
  .res-header-side { text-align: right; flex-shrink: 0; }
  .res-buffer-label { font-size: .62rem; letter-spacing: .14em; text-transform: uppercase; color: #a8885a; font-weight: 500; }
  .res-buffer-val { font-size: 1.15rem; font-weight: 500; letter-spacing: -.01em; margin-top: 2px; }

  .res-badge { font-size: .75rem; padding: 6px 12px; border-radius: 2px; line-height: 1.4; font-weight: 400; }
  .res-badge--ok   { background: #EAF3DE; color: #27500A; border: 1px solid rgba(39,80,10,.2); }
  .res-badge--warn { background: #FAEEDA; color: #633806; border: 1px solid rgba(99,56,6,.2); }

  .res-metrics { display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px; }
  .res-metric { background: rgba(26,51,39,.03); border: 1px solid rgba(26,51,39,.08); border-radius: 2px; padding: 12px 10px 10px; text-align: center; transition: border-color .2s; }
  .res-metric.rmc--green { background: rgba(29,158,117,.06); border-color: rgba(29,158,117,.2); }
  .res-metric.rmc--amber { background: rgba(212,163,42,.06); border-color: rgba(212,163,42,.2); }
  .res-metric.rmc--red   { background: rgba(226,75,74,.06);  border-color: rgba(226,75,74,.2); }
  .rmc-val   { font-size: .98rem; font-weight: 500; color: #1a3327; letter-spacing: -.01em; }
  .rmc-label { font-size: .6rem; letter-spacing: .12em; text-transform: uppercase; color: #a8885a; margin-top: 4px; font-weight: 500; }
  .rmc-sub   { font-size: .65rem; color: #7a6e65; margin-top: 3px; }
  .res-metric.rmc--green .rmc-val { color: #1D9E75; }
  .res-metric.rmc--amber .rmc-val { color: #b07a10; }
  .res-metric.rmc--red   .rmc-val { color: #E24B4A; }

  .res-chart-row { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
  .res-chart-card { background: rgba(26,51,39,.02); border: 1px solid rgba(26,51,39,.08); border-radius: 2px; padding: 14px; }
  .rcc-title { font-size: .62rem; letter-spacing: .14em; text-transform: uppercase; color: #a8885a; font-weight: 500; margin-bottom: 10px; }
  .rcc-footnote { font-size: .67rem; color: #7a6e65; font-style: italic; margin-top: 8px; line-height: 1.5; }
  .rcc-market-note { font-size: .67rem; color: rgba(26,51,39,.45); background: rgba(201,169,110,.06); border: 1px solid rgba(201,169,110,.15); border-radius: 2px; padding: 7px 10px; margin-top: 6px; line-height: 1.5; }

  .gauge-wrap { display: flex; justify-content: center; }
  .gauge-legend { display: flex; align-items: center; gap: 4px; font-size: .68rem; color: #7a6e65; justify-content: center; margin-top: 6px; flex-wrap: wrap; }
  .gauge-dot { display: inline-block; width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }

  .res-collapsible { border: 1px solid rgba(26,51,39,.08); border-radius: 2px; }
  .res-collapsible + .res-collapsible { margin-top: 0; }
  .res-coll-hd { display: flex; align-items: center; justify-content: space-between; padding: 10px 14px; cursor: pointer; list-style: none; font-size: .65rem; font-weight: 500; letter-spacing: .14em; text-transform: uppercase; color: #a8885a; user-select: none; }
  .res-coll-hd::-webkit-details-marker { display: none; }
  .res-coll-hd svg { width: 14px; height: 14px; stroke: currentColor; fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; transition: transform .2s; flex-shrink: 0; }
  .res-collapsible[open] > .res-coll-hd svg { transform: rotate(180deg); }
  .res-coll-body { padding: 0 14px 14px; border-top: 1px solid rgba(26,51,39,.06); }

  /* ── P&L Statement table ─────────────────────────────────────────────────── */
  .pl-table { width: 100%; border-collapse: collapse; font-size: .8rem; font-family: 'DM Sans', system-ui, sans-serif; }
  .pl-table tr { border-bottom: 1px solid rgba(26,51,39,.05); }
  .pl-table td { padding: 4px 6px; color: #1a3327; vertical-align: middle; }
  .pl-table td:first-child { color: rgba(26,51,39,.7); }
  .pl-amount { text-align: right; font-variant-numeric: tabular-nums; white-space: nowrap; min-width: 80px; }
  .pl-cost { color: #b32020 !important; }
  .pl-section-hd td { font-size: .63rem; font-weight: 500; letter-spacing: .15em; text-transform: uppercase; color: rgba(26,51,39,.35) !important; padding-top: 14px; padding-bottom: 3px; }
  .pl-divider td { border-top: 1px solid rgba(26,51,39,.08); padding: 0; height: 1px; }
  .pl-divider--thick td { border-top-width: 2px; border-top-color: rgba(26,51,39,.15); }
  .pl-subtotal td { font-weight: 500; color: #1a3327 !important; }
  .pl-net td { font-weight: 600; font-size: .88rem; padding: 5px 6px; }
  .pl-kpi td { color: rgba(26,51,39,.6) !important; font-size: .78rem; }
  .pl-green { color: #1D9E75 !important; }
  .pl-red   { color: #E24B4A !important; }
  .ii-metrics { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; margin-top: 12px; }

  /* 1/3 Rule Validator */
  .rule-card { padding: 12px 14px; }
  .rule-card-hd { display: flex; align-items: baseline; gap: 10px; margin-bottom: 10px; }
  .rule-target { font-size: .6rem; color: rgba(26,51,39,.4); letter-spacing: .04em; }
  .rule-legend { display: flex; gap: 16px; margin-bottom: 8px; }
  .rule-legend-item { display: flex; align-items: center; gap: 5px; }
  .rule-swatch { display: inline-block; width: 8px; height: 8px; border-radius: 2px; background: rgba(26,51,39,.15); flex-shrink: 0; }
  .rule-legend-label { font-size: .68rem; color: rgba(26,51,39,.55); }
  .rule-legend-pct { font-size: .72rem; font-weight: 600; color: #1a3327; font-variant-numeric: tabular-nums; }
  .rule-bar { display: flex; height: 22px; border-radius: 3px; overflow: hidden; background: rgba(26,51,39,.06); gap: 2px; }
  .rule-segment { position: relative; height: 100%; transition: width .3s ease, background-color .3s ease; cursor: default; }
  .rule-segment::after { content: attr(data-tooltip); position: absolute; bottom: calc(100% + 6px); left: 50%; transform: translateX(-50%); background: #1a3327; color: #fff; font-size: .68rem; font-family: 'DM Sans', system-ui, sans-serif; font-variant-numeric: tabular-nums; padding: 3px 8px; border-radius: 3px; white-space: nowrap; pointer-events: none; opacity: 0; transition: opacity .15s; z-index: 10; }
  .rule-segment:hover::after { opacity: 1; }
  .rule-verdict-row { display: flex; justify-content: flex-end; margin-top: 8px; }
  .rule-verdict { font-size: .68rem; font-weight: 600; letter-spacing: .1em; text-transform: uppercase; padding: 3px 10px; border-radius: 2px; }
  .rule-verdict--healthy   { background: rgba(29,158,117,.12); color: #1D9E75; }
  .rule-verdict--marginal  { background: rgba(212,163,42,.12);  color: #b07a10; }
  .rule-verdict--walkaway  { background: rgba(179,32,32,.1);    color: #b32020; }

  @media (max-width: 1100px) { .calc-layout { grid-template-columns: 1fr; } .calc-panel { position: static; max-height: none; overflow-y: visible; } }
  @media (max-width: 800px)  { .res-metrics { grid-template-columns: repeat(2, 1fr); } .res-chart-row { grid-template-columns: 1fr; } }
  @media (max-width: 600px)  { .portal-content { padding: 32px 5vw 60px; } .field-row { grid-template-columns: 1fr; } .calc-panel, .output-panel { padding: 18px 14px; } .res-metrics { grid-template-columns: repeat(2, 1fr); } }
</style>
